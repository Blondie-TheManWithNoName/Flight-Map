import React from "react";

const pointSz = 1.5;

export const CityPoint = (props) => {
  return (
    <div>
      {/* <input type="checkbox" id={props.id} className="check" />
      <label
        htmlFor={props.id}
        className="dot"
        style={{
          top: props.y - pointSz / 2 + "%",
          left: props.x - pointSz / 2 + "%",
        }}
      >
        <span className="airport-name">{props.id}</span>
      </label> */}
    </div>
  );
};
